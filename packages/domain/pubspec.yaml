name: domain
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.2.3 <4.0.0"

description: Domain layer (business models, repository interfaces).

dependencies:
  core:
    path: ../core
  freezed_annotation: ^3.1.0
  equatable: ^2.0.5
  flutter_hooks: ^0.21.2
  hooks_riverpod: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.0
  build_runner: ^2.7.1
  freezed: ^3.1.0
# Keep flutter section minimal / omitted for pure Dart domain package.
