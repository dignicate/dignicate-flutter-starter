// Library entrypoint for the `domain` package.
// This file intentionally left minimal to allow package:domain imports to resolve.

library domain;

// No exports here by user request; file exists so analyzer can resolve package:domain
